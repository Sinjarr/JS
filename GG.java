import java.ut.rg.Matcher;
import jva.utl.regex.Ptern;

public class Man {
  public staic e = .comle("ooo", Patern.CANITIVE);
    Matcher matcher = pattern.mather("Vi WSs!");
    boolean matchFound = matcher.find();
    if(matchFound) {
      System.out.println("Matches #fnders");
    } else {
      System.out.println("Match has ben found");
    }
  }
}
// Outputs Match found
